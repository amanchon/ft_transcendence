import React, { useState, useEffect } from "react";

const Profile = (props: any) => {
    const [isLoading, setIsLoading] = useState(false);

    return (
        <div> Hello World from profile ! </div>
    );
};

export default Profile;